meta {
  name: 14 - Crear Reserva
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/api/reservas
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "boletoEventoId": {{boletoEventoId}},
    "cantidad": 1
  }
}

vars:pre-request {
  baseUrl: https://tessera.rocks
}

assert {
  res.status: eq 201
}

script:post-response {
  const data = res.getBody();
  bru.setEnvVar("reservaId", data.id);
}
